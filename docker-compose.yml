services:
  neo4j:
    image: neo4j:5
    container_name: fydo-neo4j
    ports:
      - '7474:7474' # browser UI
      - '7687:7687' # bolt
    environment:
      NEO4J_AUTH: neo4j/fydo-graph
    volumes:
      - neo4j-data:/data

volumes:
  neo4j-data:
